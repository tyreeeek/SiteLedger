import SwiftUI

struct ContentView: View {
    var body: some View {
        // This view is not used - the app routes through SiteLedgerApp.swift
        // based on authentication state and user role
        EmptyView()
    }
}

#Preview {
    ContentView()
}
